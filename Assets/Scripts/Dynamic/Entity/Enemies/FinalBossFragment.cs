using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FinalBossFragment : MonoBehaviour
{
    [SerializeField] List<EnvironmentDynamicPhysics> EnvironmentObjects;

    enum FragmentState { INACTIVE, IDLE, CHASE, FIRING };


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
